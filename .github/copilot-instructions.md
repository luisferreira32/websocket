The repository must not contain any external dependency.
Always use the standard library.
Avoid doing functions if there is no re-usage of the code.
No comments are added to the code in-line. 
Only add comments as documentation at the top of the functions and structs.